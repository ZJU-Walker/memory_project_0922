dataset/
.claude
*.npz
__pycache__
openpi/third_party
gello_software/
data/
*.mp4
eval/
openpi/scripts/eval_results/
# Local outputs and scratch (v3.x diagnostics, training logs)
openpi/diagnostic_outputs/
openpi/training_logs/
openpi/writer_contribution_results/
.ssh_known_hosts
*.pdf
# Run artifacts and scratch (never commit: v3.5/v36 diagnostics runs are 300G+)
v35/
v4/
v5/
.tools/
openpi_test_mem/

# Local plans, handoff notes, and generated reports; keep outside the codebase.
/RUN6_PLAN*.md
/V35_CURRENT_STATUS_HANDOFF_FOR_CLAUDE*.md
/V35_PLAN_FOR_CLAUDE_REVIEW*.md
/openpi/reports/

# Historical memory handoffs, plans, diagnostics, and status reports stay local.
/V[0-9]*.md
/openpi/docs/v*.md

# v4 worktree-local read-only links into the main project.
/data
/v35_readonly
v6/
# top-level link to the frozen v5 artefacts (project_paths.SHARED_DATA_LINKS)
v5
# v7 worktree: read-only link to the v6 artefacts and the v7 artefact namespace (never commit)
v6
v7/
# RoboMME / bean-scoop artefact folders (checkpoints, datasets, rollouts, logs); scripts and docs under */logs are tracked
/robomme/checkpoints/
/robomme/data/
/robomme/rollouts/
/robomme/replay/
/robomme/cache/
/robomme/expert_val/
/robomme/diagnostics/
/beans/checkpoints/
*.log
*.out
*.pkl

# the RoboMME evaluation client is source code (the generic eval/ rule above is for run outputs)
!openpi/cluster_robomme/eval/
beans/ablations/logs/
